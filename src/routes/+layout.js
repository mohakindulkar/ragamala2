// This tells SvelteKit to pre-render every page as a real HTML file
export const prerender = true;

// This creates folders (e.g. /session/5/index.html) instead of flat files
// which is required for GitHub Pages to load sub-directories correctly.
// export const trailingSlash = 'always';